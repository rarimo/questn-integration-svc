configVersion: 1
project: "backend"
---
image: builder
from: golang:1.19-alpine
docker:
  WORKDIR: /go/src/gitlab.com/rarimo/questn-integration-svc
git:
  - add: /
    to: /go/src/gitlab.com/rarimo/questn-integration-svc
    stageDependencies:
      install:
        - go.mod
        - go.sum
      setup:
        - "**/*"
shell:
  install:
    - apk add git build-base
    - git config --global url."https://gitlab-ci-token:{{ env "CI_JOB_TOKEN" }}@gitlab.com/".insteadOf https://gitlab.com/
    - echo "machine gitlab.com login gitlab-ci-token password {{ env "CI_JOB_TOKEN" }}" > ~/.netrc
    - go env -w GOPRIVATE=gitlab.com/*
    - go env -w GONOSUMDB=gitlab.com/*
    - go env -w GONOPROXY=gitlab.com/*
    - cd /go/src/gitlab.com/rarimo/questn-integration-svc
    - go mod download
    - go mod vendor
  setup:
    - export CGO_ENABLED=1
    - export GO111MODULE=off
    - export GOOS=linux
    - go build  -o /usr/local/bin/questn-integration-svc /go/src/gitlab.com/rarimo/questn-integration-svc

---
image: service
from: alpine:3.14.6
docker:
  ENTRYPOINT: questn-integration-svc
shell:
  setup:
    - apk add --no-cache ca-certificates
import:
  - image: builder
    add: /usr/local/bin/questn-integration-svc
    to: /usr/local/bin/questn-integration-svc
    after: setup
